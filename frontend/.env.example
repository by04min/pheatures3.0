# Copy this file to .env.production and fill in your deployed backend URL.
# Leave unset in development — Vite's proxy (/api → localhost:5000) will be used instead.
VITE_API_BASE_URL=https://your-backend.onrender.com/api
